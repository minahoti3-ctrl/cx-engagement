import { ComingSoon } from "@/app/components/ComingSoon";

export default function OrgPlaceholder() {
  return (
    <ComingSoon
      pageNum={5}
      pageLabel="CX org evolution"
      description="July → beyond runway. A 5-month timeline you can drag pinned commitments onto, plus shared notes from the room."
    />
  );
}
