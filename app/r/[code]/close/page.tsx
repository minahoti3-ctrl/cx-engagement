import { ComingSoon } from "@/app/components/ComingSoon";

export default function ClosePlaceholder() {
  return (
    <ComingSoon
      pageNum={7}
      pageLabel="Close"
      description="Thank you for driving the CX transformation. Decisions and next steps summarized, plus space for one takeaway from each person in the room."
    />
  );
}
