import { ComingSoon } from "@/app/components/ComingSoon";

export default function BolderPlaceholder() {
  return (
    <ComingSoon
      pageNum={6}
      pageLabel="From bold to bolder"
      description="How do we keep an eye on the future while executing Bold today? Three RAG readiness signals, plus trigger submissions everyone can rate red / amber / green."
    />
  );
}
